Use the review-code skill (`shared/skills/review-code/SKILL.md`).

Dùng skill review-code.

Scope: user-provided files, or `git diff --staged` / `git diff` if none given.

Scope: file user chỉ định, hoặc git diff nếu không chỉ định.
