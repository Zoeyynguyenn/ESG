Read `.planning/current_plan`, then `task_plan.md` and `workflow-state.md`.

Đọc `current_plan`, rồi `task_plan.md` và `workflow-state.md`.

Reply with: goal, current phase, blockers, next step (max 5 lines).

Trả lời: goal, phase hiện tại, blocker, bước tiếp theo (tối đa 5 dòng).
